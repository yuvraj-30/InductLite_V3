# Vendored extract-zip

This is a local copy of `extract-zip` with the `yauzl` dependency bumped to
`^3.2.1` so the Puppeteer browser-downloader chain no longer pulls the
vulnerable `yauzl@2.x` release line.
